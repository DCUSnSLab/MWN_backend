[uwsgi]
module = app:app
master = true

# Use a single process with threads to avoid scheduler duplication
processes = 1
threads = 4
enable-threads = true
single-interpreter = true

socket = 0.0.0.0:5000
vacuum = true
die-on-term = true
buffer-size = 65535
